# プロジェクト作業規則

## GitHub反映

- 機能追加・機能修正では、関連文書の更新、テストまたはビルド、差分確認、コミット、現在の作業ブランチのGitHubへのpushまでを通常の完了条件とする。利用者から明示的に止められた場合はpushしない。
- push前に、対象リポジトリ、remote、ブランチ、送信対象の差分を確認する。利用者の無関係な変更、認証情報、秘密情報をコミットへ混ぜない。
- テスト失敗、認証失敗、remote不一致がある場合はpushせず、ローカル変更を保持して状況を報告する。
- 通常の工程許可は作業ブランチへのpushまでを対象とし、main反映、ブランチ削除、GitHub Release、タグ、本番公開の許可とは扱わない。
- 利用者が本案件について正式リリースを明示的に許可した場合だけ、テスト・ビルド・CI、文書、秘密情報、競合、自動デプロイ影響を再確認し、通常マージまたはPRでmainへ反映する。
- remote mainに対象コミットが含まれることを確認してからremote作業ブランチを削除し、mainへ切り替えてfast-forwardで同期してからローカル作業ブランチを削除する。途中で失敗した場合は削除せず作業ブランチを保持する。
- force pushと履歴改変は禁止する。`main`、`develop`、`release`などの恒久ブランチは削除しない。GitHub Release、タグ、本番公開はmain反映とは別の明示許可を必要とする。
- 保護ブランチへ直接pushできない場合は作業ブランチをpushし、PRが必要であることを報告する。
