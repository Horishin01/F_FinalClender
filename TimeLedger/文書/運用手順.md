# 運用手順

## 開発DBの初期化・更新

1. `dotnet restore` を実行する。
2. `dotnet run --project TimeLedger.csproj` で起動する。

開発DBはTimeLedger直下の `timeledger.db` に保存され、SQLiteの `timeledger.db-wal` と `timeledger.db-shm` も同じ場所に作られる。初回起動時にテーブルが自動作成される。削除はデータを失うため、必要な場合は3ファイルをコピーしてから行う。
