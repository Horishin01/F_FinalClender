@model TimeLedger.Models.AppNotice
@{
    ViewData["Title"] = "アップデート / 障害の追加";
    ViewData["BodyClass"] = "admin-portal";
}

<div class="container-xl">
    <h1 class="hero-title mb-3">アップデート / 障害の追加</h1>
    <form asp-action="Create" method="post" class="card p-3 shadow-sm">
        @Html.AntiForgeryToken()
        <partial name="_Form" />
        <div class="d-flex justify-content-between mt-3">
            <a class="btn btn-outline-secondary" asp-action="Index">一覧へ戻る</a>
            <button type="submit" class="btn btn-primary">保存</button>
        </div>
    </form>
</div>
