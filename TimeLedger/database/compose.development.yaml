name: timeledger-development-db

services:
  postgres:
    image: postgres:16.15
    container_name: timeledger-postgres-development
    environment:
      POSTGRES_DB: ${POSTGRES_DB:?database/config/development.env の POSTGRES_DB が未設定です}
      POSTGRES_USER: ${POSTGRES_USER:?database/config/development.env の POSTGRES_USER が未設定です}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?database/config/development.env の POSTGRES_PASSWORD が未設定です}
      POSTGRES_INITDB_ARGS: --encoding=UTF8
    ports:
      - "127.0.0.1:55432:5432"
    volumes:
      - type: bind
        source: ./runtime/development/data
        target: /var/lib/postgresql/data
      - type: bind
        source: ./runtime/development/backups
        target: /backups
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s
    restart: unless-stopped
    stop_grace_period: 1m
