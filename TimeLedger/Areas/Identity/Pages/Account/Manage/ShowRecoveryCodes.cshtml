@page
@model ShowRecoveryCodesModel
@{
    ViewData["Title"] = "バックアップコード";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<div class="settings-card">
    <h2>@ViewData["Title"]</h2>
    <p class="settings-description">下記のコードは一度だけ表示されます。紙やパスワードマネージャーに保管してください。</p>

    <partial name="_StatusMessage" for="StatusMessage" />

    <div class="danger-zone mb-3">
        <p><strong>デバイスを紛失し、コードも手元にない場合はアカウントへアクセスできなくなります。</strong></p>
    </div>

    <div class="form-field">
        @for (var row = 0; row < Model.RecoveryCodes.Length; row += 2)
        {
            <code class="me-3">@Model.RecoveryCodes[row]</code>
            <code>@Model.RecoveryCodes[row + 1]</code>
            <br />
        }
    </div>
</div>
