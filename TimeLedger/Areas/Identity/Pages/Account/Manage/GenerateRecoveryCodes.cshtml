@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "バックアップコードの再生成";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<div class="settings-card">
    <h2>@ViewData["Title"]</h2>
    <p class="settings-description">端末紛失時にサインインできるよう、バックアップコードを新しく発行します。</p>

    <partial name="_StatusMessage" for="StatusMessage" />

    <div class="danger-zone">
        <p><strong>コードは安全な場所に保管してください。紛失した場合、アカウントへアクセスできなくなる恐れがあります。</strong></p>
        <p>Authenticator アプリのキーは変更されません。キー自体を変更したい場合は <a asp-page="./ResetAuthenticator">Authenticator をリセット</a>してください。</p>
    </div>

    <form method="post" class="form-actions">
        <button class="btn-primary-modern" type="submit">コードを再生成</button>
    </form>
</div>
