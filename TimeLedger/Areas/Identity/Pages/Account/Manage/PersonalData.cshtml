@page
@model PersonalDataModel
@{
    ViewData["Title"] = "個人データ";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<div class="settings-card">
    <h2>@ViewData["Title"]</h2>
    <p class="settings-description">登録済みの個人データをダウンロードするか、アカウントを完全に削除できます。</p>

    <div class="form-actions">
        <form id="download-data" asp-page="DownloadPersonalData" method="post" class="d-inline">
            <button class="btn-outline" type="submit">データをダウンロード</button>
        </form>
        <a id="delete" asp-page="DeletePersonalData" class="btn-primary-modern">アカウントを削除</a>
    </div>

    <p class="hint-text mt-3">
        削除操作は取り消せません。エクスポートしたファイルにはメールアドレスなど、すべての登録データが含まれます。
    </p>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
