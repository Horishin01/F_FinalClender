@page
@model Disable2faModel
@{
    ViewData["Title"] = "2段階認証の無効化";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<div class="settings-card">
    <h2>@ViewData["Title"]</h2>
    <p class="settings-description">この操作は 2 段階認証のみを停止し、Authenticator アプリのキーは変更しません。</p>

    <partial name="_StatusMessage" for="StatusMessage" />

    <div class="danger-zone mb-3">
        <p>
            再度有効化する場合は、Authenticator の設定をやり直す必要があります。キー自体を変更したい場合は
            <a asp-page="./ResetAuthenticator">Authenticator のリセット</a>を実行してください。
        </p>
    </div>

    <form method="post" class="form-actions">
        <button class="btn-primary-modern" type="submit">2段階認証を無効にする</button>
    </form>
</div>
