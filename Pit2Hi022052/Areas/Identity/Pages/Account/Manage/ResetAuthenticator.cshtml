@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Authenticator キーのリセット";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<div class="settings-card">
    <h2>@ViewData["Title"]</h2>
    <p class="settings-description">Authenticator アプリの秘密キーを再生成し、アプリ設定を初期化します。</p>

    <partial name="_StatusMessage" for="StatusMessage" />

    <div class="danger-zone">
        <p>
            リセット後はアプリが使えなくなり、2段階認証も一時的に無効になります。
            <strong>必ず新しいキーでアプリを再設定してください。</strong>
        </p>
    </div>

    <form id="reset-authenticator-form" method="post" class="form-actions">
        <button id="reset-authenticator-button" class="btn-primary-modern" type="submit">キーをリセット</button>
    </form>
</div>
